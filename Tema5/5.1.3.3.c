//Crear un programa que pida al usuario 10 números, calcule su media y luego muestre los que están por encima de la media
#include<stdio.h>
int main()
{
    int numbers[10];
    int average;
    int i;
    int sum;
    
    for (i=0; i<10; i++)
    {
      printf("Input number: \n");
      scanf("%d", &  );
    }
    
    
    //for (i=0; i<=9; i++)  sum += numbers[i];
    
    
    return 0;
}